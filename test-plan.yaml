schema_version: 1
managed_by: github-test-org-factory/1.0.0
production_org: cliptown
test_org: cliptown-test
repository: desktop-clipboard-e2e
kind: desktop
readiness: ready
objectives:
- Verify clipboard capture/restore, tray, hotkeys, pinned items, history, and sensitive-item expiry on desktop across
  the supported happy-path states and canonical fixtures.
- Verify clipboard capture/restore, tray, hotkeys, pinned items, history, and sensitive-item expiry on desktop under
  retries, interruption, concurrency, offline operation, or partial failure.
- Verify clipboard capture/restore, tray, hotkeys, pinned items, history, and sensitive-item expiry on desktop preserves
  authorization, idempotency, integrity, observability, and actionable failure classification.
always_on_checks:
- Validate test-plan and dependency metadata.
- Validate generated fixture and workflow syntax.
- Classify missing upstreams or credentials separately from product regressions.
schedule_utc: 17 5 * * *
expensive: true
required_infrastructure:
- Linux runner
- macOS runner
- Windows runner
cases:
- id: TC-001
  title: Verify clipboard capture/restore, tray, hotkeys, pinned items, history, and sensitive-item expiry on desktop
    across the supported happy-path states and canonical fixtures.
  tier: scheduled-live
  status: planned
  evidence:
  - junit
  - readiness JSON
  - workflow artifacts
- id: TC-002
  title: Verify clipboard capture/restore, tray, hotkeys, pinned items, history, and sensitive-item expiry on desktop
    under retries, interruption, concurrency, offline operation, or partial failure.
  tier: scheduled-live
  status: planned
  evidence:
  - junit
  - readiness JSON
  - workflow artifacts
- id: TC-003
  title: Verify clipboard capture/restore, tray, hotkeys, pinned items, history, and sensitive-item expiry on desktop
    preserves authorization, idempotency, integrity, observability, and actionable failure classification.
  tier: scheduled-live
  status: planned
  evidence:
  - junit
  - readiness JSON
  - workflow artifacts
